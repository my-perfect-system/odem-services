---
galaxy_info:
  author: foo <bar@baz.com>
  description: "Deploy the mps-monitoring stack (Prometheus, Loki, Grafana) via docker compose"
  min_ansible_version: "2.16.0"
  license: GPL-3.0-or-later
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - debian
    - debian13
    - trixie
    - services
    - monitoring
    - prometheus
    - loki
    - grafana
    - odem-services
dependencies:
  - role: odem.base.identity
